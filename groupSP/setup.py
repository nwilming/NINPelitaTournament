from setuptools import setup

setup(name='groupsp',
      version='1',
      description='The Base Players',
      author='Sasa and Phil',
      license='MIT',
      packages=['groupsp'],
      zip_safe=False)
